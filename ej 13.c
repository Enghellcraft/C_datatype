#include<stdio.h>

int main (void) {

int a, b, sum, res, mul;
float  div, prom, ope;


printf ("Ingrese 2 numeros mayores a cero");
scanf ("%d", &a);
scanf ("%d",  &b);
(sum) = ( a + b );
(res) = (a - b);
(mul) = (a * b);
(div) = (a / b);
(prom) = ((a + b) / 2 );
(ope) = ( 2 * (a * (b / 2)));



printf ("sum es %d \n la res es %d \n la mul es %d \n la div es %.2f \n el prom es %.2f \n la oper es %.2f", sum, res, mul, div, prom, ope);
}
